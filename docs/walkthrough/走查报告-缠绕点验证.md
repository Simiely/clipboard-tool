# 针对性走查报告 · 缠绕点验证(2026-08-15)

> 依据《场景走查》v3 方法论,素材见 `docs/walkthrough/scenarios.md`,能力清单见 `docs/walkthrough/capabilities.md`
> 基线:v0.6.0(HEAD `8f86002`)· 方式:逐条读原文核对(文件:行号),快速链 grep 确认
> 背景:验证《架构清晰度评估报告》的 8 处"主线×支线缠绕点"是**结构味道**还是**真实缺陷**

## 总体结论

- **10 条链路走查:9 通 1 不通**(不通 = S-03 删除失败仍提示"已删除",真缺陷)
- 8 处缠绕点中:**7 处确认为"结构味道"(行为正确,仅可维护性差),1 处暴露真实缺陷**(P-101 由缠绕点 F-13 的删除路径引出)
- 1 个 🔴 真缺陷(风险 12,必修)+ 3 个 ⚪ 低危(建议顺手修)

## 问题清单

| 编号 | 级别 | 问题 | 位置 | 受影响链 | 风险分(L×I) | 影响 |
|---|---|---|---|---|---|---|
| P-101 | 🔴 真缺陷 | **删除失败仍提示"已删除"**:`makeDeleteBtn` 中 `api(...).catch(errToast)` 吞掉异常后**无条件** `flash("已删除")`+`refreshList()`,网络错误/会话失效时用户看到成功提示但条目还在 | public/app.js L682-686 | S-03, S-01 | 3×4=**12** | 误报成功,误导用户;删除失败无感知 |
| P-102 | ⚪ 低危 | **未配置 WebDAV 的用户每次删除也写墓碑文件**:`deleteClip` 无条件 `recordTombstone`,产生 `<uid>.tombstones.json`(每条删除一次写盘)。语义上墓碑只服务 WebDAV 同步 | lib/core/clips.js L396-405 + L77-81 | S-02 | 2×1=2 | 磁盘多一个文件,量级可忽略;纯属缠绕点 1 的物理体现 |
| P-103 | ⚪ 低危 | **搜索拼音仅前端实现**:搜索框输入(前端 `renderList` L624)拼音可命中;URL `?q=sf` / 刷新后 `loadClips` 带 q(后端 `listClips` L249-256)只做子串匹配,拼音不命中。日常搜索走前端无感,仅"直链/刷新"场景语义不一致 | public/app.js L615-626 vs lib/core/clips.js L249-256 | S-04 | 1×2=2 | 语义漂移风险,双实现维护成本 |
| P-104 | ⚪ 低危 | **自动同步失败静默**:`runAutoSync` catch 吞错,前端无"上次自动同步失败"指示;用户误以为数据已备份 | lib/core/webdav.js L296 | S-09 | 2×2=4 | 数据备份信任风险(低概率) |

## 覆盖矩阵结论

- 无整行/整列空白;L4(S-02/S-05/S-06)、L5(S-09)、L7(S-08)、L8(S-10)、L9(S-03)均覆盖 ✅
- 盲区说明:「故障注入」列只有删除链路(S-03),编辑/存入的失败路径未专项注入——但走查中发现 `openEditModal`(L1240)与 `savePasteContent`(L1139)均用 try/catch + errToast 且**无成功提示**,不存在 P-101 同类问题(成功提示仅删除链有),故不补素材

## 走查验证的良好点(程序扎实之处)

1. **8 处缠绕点中 7 处行为正确**:boot 监听器只绑一次、弹窗防重复(`$(".mask")`/`$(".paste-modal")` 双防线)、pendingHtml 生命周期闭环(每次打开重置 + 存后清空)、suppressAutoPasteUntil 800ms 窗口与 clipboardchange 时序匹配、防连点 guard 覆盖到位
2. **删除/清理路径无残留**:deleteUser 清理 5 类文件(条目/归档/文件/tombstones/webdav 配置);clearAllClips 清空归档+墓碑语义正确(清空不传播删除)
3. **其余删除路径返回值检查一致**:删用户/删标签/置顶/JSON 覆盖全部 `if (r)` 守卫——**唯独 makeDeleteBtn 是唯一例外**,这反而说明是"漏网"而非"风格"
4. **WebDAV 合并算法** `mergeSnapshots`(webdav.js L169)纯函数、墓碑裁决(删 vs 改)逻辑正确;`autoSyncInProgress` 防重入
5. 素材与程序一致性:所有素材引用的入口/字段均可对照 capabilities.md 落实,无虚构功能

## 结论三问

1. **链路逻辑通顺吗?** 通顺。10 条链 9 通;主线(存入→列表→复制/删除/编辑)清晰无绕路
2. **是补丁叠补丁吗?** 否。权限(requireAuth 单点)、限流(loginGuard)、防重入(Set)、状态(pendingHtml/suppressAutoPasteUntil)各有单点归属;缠绕点多为"文件级归并"问题,非逻辑叠补丁
3. **会无法操作/出 bug 吗?** 会——P-101 删除失败误报成功(必修);其余为低危可维护性问题

## 建议处理(按风险分排序)

| 优先级 | 事项 | 内容 | 工作量 |
|---|---|---|---|
| 🔴 必修 | 修 P-101 | `makeDeleteBtn`:改 `const r = await api(...).catch(...); if (r) { flash("已删除"); refreshList(); }`(与 L578 标签删除同风格) | 1 行,零风险 |
| 🟡 建议 | 修 P-104 | `runAutoSync` 失败时把 lastError 写入 `<uid>.webdav.json`,前端 renderWebdavSection 显示「上次自动同步失败:xx」 | 小 |
| ⚪ 可选 | 修 P-102 | 墓碑写入改为「仅当存在 WebDAV 配置时记录」(webdav.js 的 getSyncConfig 存在才记),或接受现状(量级可忽略) | 小 |
| ⚪ 可选 | 修 P-103 | 二选一:后端 listClips 增加拼音匹配(需把 3755 表放服务端,违反"拼音只在前端"现状);或文档注明"拼音搜索仅前端生效" | 中 |

**修复闭环建议**:P-101 修复后跑冒烟集(`npm test` 三套,重点 smoke 的删除用例)+ 手动回归 S-03(断网删除不误报);P-102 若做需重跑 test-webdav-sync(墓碑断言);P-104 加字段不破坏现有同步语义,跑 test-auto-sync 即可。

## 修复状态(2026-08-15 v0.6.1 已落地)

| 编号 | 状态 | 修复内容 | 回归验证 |
|---|---|---|---|
| P-101 | ✅ fixed+verified | makeDeleteBtn 加 `if (r)` 守卫(app.js L682-687) | 冒烟 34/34 |
| P-102 | ✅ fixed+verified | deleteClip 返回 tombstone,路由层按 WebDAV 配置记录(clips.js/routes-clips.js) | WebDAV 19/19 + 实证无配置不落盘 |
| P-103 | ✅ 文档注明 | README/AGENTS 注明拼音仅前端生效 | — |
| P-104 | ✅ fixed+verified | runAutoSync 记 lastSyncError + GET config 返回 + 前端展示 | 自动同步 3/3 + 实证失败落盘 |

## 与架构评估报告的联动

- 评估报告「缠绕点 F-13(makeDeleteBtn)」的结论从"组装中心过载"升级为**真缺陷 P-101**——走查补充了评估遗漏的行为层验证
- 其余缠绕点(墓碑位置、boot 过载、openPasteModal 过载、全局状态、搜索双实现)维持"结构味道"定性,仍按评估报告 A1-A4/B1-B3 计划推进拆分,拆分不改行为、回归以本报告 10 条链为基线
